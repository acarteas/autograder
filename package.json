{
  "name": "autograder",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^6.0.2",
    "react-scripts": "^4.0.3"
  },
  "scripts": {
    "start": "concurrently \"cd server && npm start\" \"cd client && npm start\" \"cd data && npm start\""
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {}
}
